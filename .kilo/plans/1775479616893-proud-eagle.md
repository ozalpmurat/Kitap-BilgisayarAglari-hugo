# Plan: Create AGENTS.md for Hugo Documentation Site

## Analysis Summary

This is a **Hugo static site** (educational content about computer networks in Turkish). It uses:
- **hugo-theme-relearn** theme
- **Hugo** for building
- **.kilo/** directory with kilocode plugin (for Kilo CLI)
- No Python/JS source code - content is in Markdown files
- No traditional build/test/lint commands for code

## Findings

- **No Cursor rules** (.cursor/rules/ or .cursorrules)
- **No Copilot rules** (.github/copilot-instructions.md)
- **No existing AGENTS.md** in project root
- **.kilo/command/*.md and .kilo/agent/*.md** don't exist yet
- Hugo is the only build tool - commands like `hugo server`, `hugo build`

## Plan

Create AGENTS.md (~150 lines) containing:

### 1. Build Commands
- `hugo server` - development server
- `hugo build` - production build
- `hugo -D` - include drafts

### 2. Code Style Guidelines (adapted for Hugo/Markdown)
- Frontmatter format
- Markdown syntax
- Image paths
- File naming conventions
- Content organization

### 3. Project Structure
- content/ directory structure
- Theme configuration in hugo.toml

## Output

Write plan to: /home/murat/github/Kitap-BilgisayarAglari-hugo/.kilo/plans/1775479616893-proud-eagle.md